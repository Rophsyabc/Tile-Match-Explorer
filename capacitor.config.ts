import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.soardev.tilematchexplorer',
  appName: 'Tile Match Explorer',
  webDir: 'dist'
};

export default config;
